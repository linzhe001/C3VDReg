# C3VDReg

Full dataset and checkpoint download links are withheld during double-blind review and will be released after the review period. A tiny 8,192-point smoke-test subset is committed for interface checks only.

This repository is the code/configuration companion for the BMVC2026 paper, titled `C3VDReg: A Benchmark for Local-to-Local Colonoscopic Registration toward Anatomical Localization`.

C3VDReg builds a controlled local-to-local colonoscopic point cloud registration benchmark from C3VD depth maps, camera poses, and CT meshes. For each C3VD frame, the source point cloud is reconstructed from the colonoscopic depth frame, while the target point cloud is generated by raycasting the CT mesh from the matched camera pose.

The repository keeps the runnable benchmark framework, paper configs, C3VDReg manifest, and HPT utilities. It does not vendor baseline source code, model weights, raw C3VD data, paper drafts, generated figures, or runtime outputs.

## Paper Protocol

C3VDReg isolates rigid partial-to-partial registration before the harder full local-to-global localization problem. The raycast target gives the oracle visible CT surface for the same frame, so the benchmark tests whether a method can align a video-depth source cloud to the corresponding visible CT target cloud. It does not evaluate full CT surface retrieval, nonrigid deformation, unmatched viewpoints, tissue motion, sequence tracking, or clinical deployment.

Main BMVC draft protocol:

| Item | Setting |
| --- | --- |
| Dataset | C3VD-derived raycasting pairs |
| Manifest | `manifests/c3vd_raycasting_v1.jsonl` |
| Pairs | 10,015 total: 5,952 train, 1,975 val, 2,088 test |
| Held-out test scenes | `cecum_t1_a`, `cecum_t3_a`, `sigmoid_t3_b`, `trans_t1_a`, `trans_t2_b`, `trans_t4_a` |
| Input budget | 8,192 source points and 8,192 target points |
| Pose convention | source-to-target transform |
| Eval perturbation | R25-90deg/T100-500mm, source only, no added noise |
| Train perturbation configs | R0-90deg/T0-500mm, source only, 0-200 epochs |
| Primary metrics | RR@5 at 5deg/5mm and RR@10 at 10deg/10mm |
| Error metrics | RRE in degrees, RTE in millimetres |

The paper reports that this controlled task is still not solved by current public methods. GeoTransformer is the strongest evaluated baseline, but reaches only 17.43% RR@5 and 35.63% RR@10. Test-set GT overlap is high, yet recall remains low and nonmonotonic with overlap, pointing to translation ambiguity in repetitive tubular anatomy rather than missing shared surface alone.

## Repository Layout

Remote repository contents:

- `configs/`: benchmark dataset, preprocess, model, paper evaluation, 0-200 epoch training, and HPT config files.
- `manifests/`: portable C3VDReg manifest with paths relative to `data.dataset_root`.
- `sample_data/`: tiny 8,192-point C3VDReg smoke-test subset, not a benchmark substitute.
- `requirements.txt`: Python dependency list.
- `scripts/`: train/eval entry points, manifest builder, and minimal HPT helper CLIs.
- `src/`: benchmark runners, adapters, preprocessing, metrics, reporting, HPT utilities, and shared utilities.
- `README.md`: BMVC2026 paper companion instructions.

## Setup

```bash
git clone https://github.com/linzhe001/C3VDReg.git
cd C3VDReg
python -m pip install -r requirements.txt
```

Install the PyTorch build that matches the local CUDA driver before running GPU baselines. Vendor baselines such as RegTR and GeoTransformer may require their own extension compilation after cloning the original repositories.

## Data

The full C3VDReg data release link is withheld during double-blind review and will be released after the review period.

This repository includes a small smoke-test subset under `sample_data/c3vdreg_tiny_8192/`. It contains 6 local-to-local pairs, with 2 train, 2 val, and 2 test pairs. Each source and target point cloud has 8,192 points. This subset is only for verifying the loader and CLI wiring; it is not used for the BMVC benchmark numbers.

Run the tiny ICP evaluation without downloading checkpoints:

```bash
python scripts/runners/eval_benchmark.py \
  --config configs/benchmark/tiny_8192/eval_icp.yaml
```

Run the tiny GeoTransformer checkpoint evaluation after restoring the released
GeoTransformer checkpoint. Run this from the C3VDReg Python environment:

```bash
python scripts/runners/eval_geotransformer_tiny.py \
  --checkpoint /path/to/geotransformer_c3vd_model_best.pth \
  --build-baseline
```

The script clones the original GeoTransformer repository into
`baselines/GeoTransformer` if it is missing, keeps that external repository
untracked, copies the checkpoint to
`checkpoints/geotransformer/geotransformer_c3vd_model_best.pth`, runs
`configs/benchmark/tiny_8192/eval_geotransformer.yaml`, and writes local result
tables plus visualizations under `outputs/benchmark/tiny_8192/geotransformer_eval/`.
The external baseline source is the original GeoTransformer code repository:
`https://github.com/qinzheng93/GeoTransformer`.

The committed manifest uses relative paths, for example:

```text
C3VD_ply_source/<scene>/<frame>_depth_pcd.ply
visible_point_cloud_ply_depth/<scene>/frame_<frame>_visible.ply
C3VD_ref/<scene>/coverage_mesh.ply
```

Configs currently default to:

```text
/mnt/f/Datasets/C3VD_sever_datasets
```

On another machine, update `data.dataset_root` in the YAML config or copy the config and point it to the local C3VD raycasting data root.

## Baselines

Baseline source code is not committed. Clone each original repository into the expected local path before training or evaluation.

| Paper label | Model id | Original code repository | Expected local source path |
| --- | --- | --- | --- |
| ICP | `icp` | classical baseline implemented in this benchmark | none |
| DCP | `dcp` | `https://github.com/WangYueFt/dcp` | `baselines/dcp` |
| PointNetLK | `pointnetlk` | `https://github.com/hmgoforth/PointNetLK` | `baselines/PointNetLK` |
| PointNetLK Revisited | `pointnetlk_revisited` | `https://github.com/Lilac-Lee/PointNetLK_Revisited` | `baselines/PointNetLK_Revisited` |
| PointNetLK-Mamba | `mamba3d_mamba2_direct` | C3VDReg fork based on PointNetLK; place fork locally | `forks/PointNetLK_c3vd` |
| RegTR | `regtr` | `https://github.com/yewzijian/RegTR` | `baselines/RegTR` |
| GeoTransformer | `geotransformer` | `https://github.com/qinzheng93/GeoTransformer` | `baselines/GeoTransformer` |

Keep cloned baselines as independent repositories. Do not vendor their source trees into C3VDReg history.

## Checkpoints

The checkpoint bundle release link is withheld during double-blind review and will be released after the review period. After obtaining the released bundle, copy its contents into `checkpoints/` before evaluation:

```bash
mkdir -p checkpoints
rsync -av /path/to/C3VDReg_checkpoint/ checkpoints/
(cd checkpoints && sha256sum -c SHA256SUMS.txt)
```

Expected checkpoint layout after restore:

| Paper label | Config path | Restored checkpoint path | Bundle path |
| --- | --- | --- | --- |
| GeoTransformer | `configs/benchmark/paper_r25_90_t100_500mm/eval_geotransformer.yaml` | `checkpoints/geotransformer/geotransformer_c3vd_model_best.pth` | `C3VDReg_checkpoint/geotransformer/geotransformer_c3vd_model_best.pth` |
| RegTR | `configs/benchmark/paper_r25_90_t100_500mm/eval_regtr.yaml` | `checkpoints/regtr/model-142848.pth` | `C3VDReg_checkpoint/regtr/model-142848.pth` |
| PointNetLK-Mamba | `configs/benchmark/paper_r25_90_t100_500mm/eval_mamba2_direct.yaml` | `checkpoints/PointNetLK_mamba/pointlk_mamba_model_best.pth` | `C3VDReg_checkpoint/PointNetLK_mamba/pointlk_mamba_model_best.pth` |
| PointNetLK Revisited | `configs/benchmark/paper_r25_90_t100_500mm/eval_pointnetlk_revisited.yaml` | `checkpoints/pointnetlk_revisited/pointnetlk_c3vd_model_best.pth` | `C3VDReg_checkpoint/pointnetlk_revisited/pointnetlk_c3vd_model_best.pth` |
| PointNetLK | `configs/benchmark/paper_r25_90_t100_500mm/eval_pointnetlk.yaml` | `checkpoints/pointnetlk/c3vd_pointnetlk_model_model_best.pth` | `C3VDReg_checkpoint/pointnetlk/c3vd_pointnetlk_model_model_best.pth` |
| DCP | `configs/benchmark/paper_r25_90_t100_500mm/eval_dcp.yaml` | `checkpoints/dcp/model_best.pth` | `C3VDReg_checkpoint/dcp/model_best.pth` |
| ICP | `configs/benchmark/paper_r25_90_t100_500mm/eval_icp.yaml` | none | none |

## Evaluation Interface

Run one paper-protocol evaluation:

```bash
python scripts/runners/eval_benchmark.py \
  --config configs/benchmark/paper_r25_90_t100_500mm/eval_geotransformer.yaml
```

Run all paper-protocol evaluations:

```bash
for cfg in configs/benchmark/paper_r25_90_t100_500mm/eval_*.yaml; do
  python scripts/runners/eval_benchmark.py --config "$cfg"
done
```

ICP can run without a checkpoint. Learning-based baselines require both the expected source path and restored checkpoint path.

## Training Interface

Run one 0-200 epoch R90/T500 training config:

```bash
python scripts/runners/train_benchmark.py \
  --config configs/benchmark/train_r90_t500mm_0_200epoch/train_geotransformer.yaml
```

Run all trainable BMVC baseline configs:

```bash
for cfg in configs/benchmark/train_r90_t500mm_0_200epoch/train_*.yaml; do
  python scripts/runners/train_benchmark.py --config "$cfg"
done
```

Available training configs:

- `configs/benchmark/train_r90_t500mm_0_200epoch/train_dcp.yaml`
- `configs/benchmark/train_r90_t500mm_0_200epoch/train_geotransformer.yaml`
- `configs/benchmark/train_r90_t500mm_0_200epoch/train_mamba2_direct.yaml`
- `configs/benchmark/train_r90_t500mm_0_200epoch/train_pointnetlk.yaml`
- `configs/benchmark/train_r90_t500mm_0_200epoch/train_pointnetlk_revisited.yaml`
- `configs/benchmark/train_r90_t500mm_0_200epoch/train_regtr.yaml`

Generated logs, generated bridge configs, checkpoints, tables, figures, and per-sample predictions should stay under `outputs/` or `checkpoints/` and remain untracked.

## HPT

`src/benchmarking/hparam_transfer/` contains Dataset-Profile-Guided Hyperparameter Transfer utilities. Use this HPT flow to quickly initialize C3VD hyperparameters from a baseline's original dataset routes while keeping benchmark-owned fields fixed.

Typical HPT flow:

1. Export or refresh the C3VD dataset profile.
2. Export baseline reference profiles from original vendor configs/code.
3. Build a context pack.
4. Generate a candidate proposal.
5. Validate the proposal and candidate configs before promotion.

The workflow contract is documented in `src/benchmarking/hparam_transfer/SKILL.md`; route cards and transfer rules live under `configs/benchmark/hparam_transfer/`.

## Reproducibility Contract

Main-table comparisons should use:

- `manifests/c3vd_raycasting_v1.jsonl`
- `configs/benchmark/paper_r25_90_t100_500mm/eval_*.yaml`
- `configs/benchmark/train_r90_t500mm_0_200epoch/train_*.yaml` when retraining
- `configs/benchmark/preprocess/canonical_v1.yaml`
- checkpoints restored from the released checkpoint bundle

Do not compare methods under different point budgets, perturbation ranges, pose conventions, or private preprocessing unless the result is explicitly labeled as a separate diagnostic rather than a BMVC main-protocol result.
